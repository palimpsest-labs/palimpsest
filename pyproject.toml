[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "palimpsest"
dynamic = ["version"]
description = "Open-source research engine for investigative journalists — persistent memory, unified search, platform APIs, and LLM synthesis"
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.10"
authors = [
    {name = "Palimpsest Contributors"},
]
keywords = ["investigative", "journalism", "research", "knowledge-graph", "cli"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Other Audience",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-cov>=5",
]

[project.scripts]
palimpsest = "palimpsest.__main__:main"

[project.urls]
Homepage = "https://github.com/jmars/palimpsest"
Repository = "https://github.com/jmars/palimpsest"
Issues = "https://github.com/jmars/palimpsest/issues"

[tool.hatch.version]
path = "src/palimpsest/__init__.py"
